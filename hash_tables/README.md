# Hash Tables

In this project, we implement a hash table in C.

It stores data using key-value pairs and uses a hash function to decide where to place each element. When two keys map to the same index, we handle it using linked lists.

This helps us understand how fast data lookup works behind structures like dictionaries.
